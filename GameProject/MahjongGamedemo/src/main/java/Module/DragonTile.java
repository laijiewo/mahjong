package Module;

public class DragonTile extends Tile {
    //中（redDragon）发（greenDragon）白（whiteDragon）
    final tileEnum type = tileEnum.Dragon;
    public DragonTile(tileEnum type) {
        super(type);
    }
}
